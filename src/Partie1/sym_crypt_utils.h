#ifndef __SYM_CRYPT_UTLS__
#define __SYM_CRYPT_UTLS__

void set_config(char* key_path, char* input_path, char* output_path, char* init_vector);

void get_config(char* key_path, char* input_path, char* output_path, char* init_vector);
 
#endif